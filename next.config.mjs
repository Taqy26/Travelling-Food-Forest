/** @type {import('next').NextConfig} */
const nextConfig = {
  // Mengizinkan HP kamu mengakses resource development Next.js
  allowedDevOrigins: ['192.168.1.7', 'localhost:3000']
};

export default nextConfig;

